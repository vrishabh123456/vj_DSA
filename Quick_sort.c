#include<stdio.h>
#include<stdlib.h>
int n;
void printf(int a[])
{
    int i;
    printf("\n");
    for(i=0;i<n;i++)
        printf("%d",a[i]);
}
void Quicksort(int a[],int first,int last)
{
    int i,j,temp,pivot;
    if(first<last)
    {
        printf(a);
        pivot = first;
        i = first, j = last;
        while(i<)
    }
